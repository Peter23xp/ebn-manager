import { Users, Clock, CircleDollarSign, TrendingUp } from 'lucide-react';
import { formatUSD } from '@/lib/utils';
export function NetworkStatsCards({ stats }: { stats?: any }) { const cards = [{ label: 'Membres actifs', value: stats?.membresActifs ?? 0, icon: Users }, { label: 'En attente', value: stats?.membresEnAttente ?? 0, icon: Clock }, { label: 'Commissions versées', value: formatUSD(stats?.commissionsVersees ?? 0), icon: CircleDollarSign }, { label: 'Promotions du mois', value: stats?.promotionsPeriode ?? 0, icon: TrendingUp }]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({ label, value, icon: Icon }) => <article key={label} className="rounded-xl border border-border bg-white p-4"><Icon size={18} className="text-primary-accent" /><p className="mt-3 text-xs text-text-muted">{label}</p><p className="mt-1 text-2xl font-bold text-text">{value}</p></article>)}</div>; }
